<div class="card m-2">
  <div class="card-body">
    <h5 class="card-title">{{user.username}}</h5>
    <h6 class="card-subtitle mb-2 text-muted">At:
      {{format_date comment_date}}</h6>
    <p class="card-text">{{comment_text}}</p>
  </div>
</div>