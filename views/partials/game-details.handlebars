<div class="col-lg-3 col-md-6 col-12">
  <div class="card text-center m-2">
    <img
      src="/images/{{filename}}"
      class="card-img-top"
      alt="..."
    />
    <div
      class="card-body d-flex flex-column align-content-center justify-content-center bg-secondary"
    >
      <h5 class="card-title">{{game_name}}</h5>
      <p class="card-text">{{genre.genre_name}}</p>
      {{!-- <p class="card-text font-weight-bold">{{postCount}}</p> --}}
      <a href="/games/{{id}}" class="btn btn-primary">See Posts</a>
    </div>
  </div>
</div>

<script src="/js/allGames.js"></script>